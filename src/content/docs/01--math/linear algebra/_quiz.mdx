import { Quiz, QuizOption } from 'starlight-videos/components';

{(props.quiz === 'linearTransformation' || props.quiz === 'all') && (
  <Quiz title="'행렬'은 단순히 숫자를 행과 열로 보관하는 구조이기만 하지는 않습니다. 약속을 통해 '행렬'에 담아놓은 특수한 연산인 행렬곱 덕분에 행렬은 *이것*으로도 볼 수 있어요. *이것*은 무엇인가요?">
    <QuizOption>좌표 변환 연산자</QuizOption>
    <QuizOption correct>선형 변환 함수</QuizOption>
    <QuizOption>벡터 공간 구조</QuizOption>
  </Quiz>
)}

{(props.quiz === 'linearity' || props.quiz === 'all') && (
  <Quiz title="'선형성'(linearity)을 이루는 성질을 모두 고르세요(2개). 이 성질이 어떻게 불리는지도 알고 있나요?" multiple>
    <QuizOption>$f(f(x))=f(x)$</QuizOption>
    <QuizOption>$x\le y \Rightarrow f(x)\le f(y)$</QuizOption>
    <QuizOption correct>$f(x+y)=f(x)+f(y)$</QuizOption>
    <QuizOption>$f(xy)=f(x)f(y)$</QuizOption>
    <QuizOption correct>임의의 스칼라 $α$에 대해, $f(αx) = αf(x)$</QuizOption>
  </Quiz>
)}


{/* props.quiz가 없으면 'all'을 기본값으로 사용 */}
{(!props.quiz || props.quiz === 'linearTransformation' || props.quiz === 'all') && (
<Quiz title="'행렬'은 단순히 숫자를 행과 열로 보관하는 구조이기만 하지는 않습니다...">
    <QuizOption>좌표 변환 연산자</QuizOption>
    <QuizOption correct>선형 변환 함수</QuizOption>
    <QuizOption>벡터 공간 구조</QuizOption>
</Quiz>
)}

{(!props.quiz || props.quiz === 'linearity' || props.quiz === 'all') && (
<Quiz title="'선형성'(linearity)을 이루는 성질을 모두 고르세요..." multiple>
    <QuizOption>$f(f(x))=f(x)$</QuizOption>
    <QuizOption>$x\le y \Rightarrow f(x)\le f(y)$</QuizOption>
    <QuizOption correct>$f(x+y)=f(x)+f(y)$</QuizOption>
    <QuizOption>$f(xy)=f(x)f(y)$</QuizOption>
    <QuizOption correct>임의의 스칼라 $α$에 대해, $f(αx) = αf(x)$</QuizOption>
</Quiz>
)}

export const MatrixMultiplicationQuiz = () => (
  <Quiz title="행렬의 곱셈에 대한 설명으로 옳은 것은?">
    <QuizOption>교환법칙이 성립한다</QuizOption>
    <QuizOption correct>결합법칙이 성립한다</QuizOption>
    <QuizOption>분배법칙이 성립하지 않는다</QuizOption>
  </Quiz>
);